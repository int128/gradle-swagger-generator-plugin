plugins {
    id 'base'
    id 'org.hidetake.swagger.generator'
}

repositories {
    jcenter()
}

dependencies {
    swaggerCodegen 'io.swagger:swagger-codegen-cli:2.4.2'
}

swaggerSources {
    petstore {
        inputFile = file("$rootDir/vendor/openapi-v2-petstore.yaml")
        code {
            language = 'spring'
            configFile = file("$rootDir/java-spring-config.json")
            templateDir = file("$rootDir/vendor/templates/java-spring")
            components = ['models', 'apis']
        }
    }
}

build.dependsOn generateSwaggerCode
